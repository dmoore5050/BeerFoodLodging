.categories_wrapper

  = link_to '#', id: 'reveal_new_cat_form', class: 'green_text bold subhead no_height' do
    Add Category
    %span.ui-icon

  = render 'new_category'

  %h2 Categories:
  %ul.category_list
    = render categories
